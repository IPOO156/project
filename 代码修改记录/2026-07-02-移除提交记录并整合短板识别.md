# 代码修改记录

- **日期**：2026-07-02
- **修改人**：nine-mirrors
- **需求来源**：菜单与模块调整：移除"审批与记录"下的"提交记录"；将"个人档案信息"中的"短板识别与改进建议"移至"职业规划"页面
- **验证结果**：
  - `npm run lint:check` 通过
  - `npm run build` 通过

---

## 一、删除记录

### 1.1 移除"审批与记录"下的"提交记录"

**涉及文件**：
- `src/app/router/routes.ts`：删除 `/approval/records` 路由
- `src/app/layouts/components/Sidebar.vue`：删除"提交记录"菜单项
- `src/features/approval/SubmitRecords.vue`：删除文件

**说明**："审批与记录"模块仅保留"待审批信息"一个子菜单。

### 1.2 从"个人档案信息"中移除"短板识别与改进建议"

**涉及文件**：`src/features/profile/ProfileInfo.vue`

移除内容：
- `AlertTriangle` 图标导入
- `weaknesses` mock 数据
- "短板识别与改进建议"卡片模板
- `.weakness-item` 相关样式

---

## 二、新增/修改记录

### 2.1 在"职业规划"页面新增"短板识别与改进建议"

**涉及文件**：`src/features/profile/CareerPlan.vue`

新增内容：
- 导入 `AlertTriangle` 图标
- `weaknesses` mock 数据
- "短板识别与改进建议"卡片，位于规划列表下方
- `.weakness-item` 相关样式

页面结构变为：
1. 职业规划与材料填写（原卡片）
2. 短板识别与改进建议（新增卡片）
3. 新增规划弹窗

---

## 三、验证命令

```bash
npm run lint:check
npm run build
```
