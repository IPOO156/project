# 代码修改记录

- **日期**：2026-07-04
- **修改人**：nine-mirrors
- **需求来源**：优化顶部标签栏交互与视觉：修复标签导航失效、实现 Edge 风格动态压缩、限制标签上限为 7 个并弹窗提示、支持图标显示与拖拽排序；补充个人中心子菜单图标；修复 TypeScript 类型检查错误。
- **验证结果**：
  - `npx eslint src/app/layouts/components/NavTabs.vue src/app/stores/tabs.ts src/shared/composables/useTabs.ts src/shared/constants/menu.ts src/app/layouts/components/Sidebar.vue src/app/layouts/DefaultLayout.vue src/features/applications/ApplicationHub.vue --max-warnings 0` 通过
  - `npx vue-tsc --noEmit` 通过
  - `npx vite build` 通过

---

## 一、删除记录

### 1.1 移除 Sidebar 菜单本地定义与 el-menu router 模式

**涉及文件**：`src/app/layouts/components/Sidebar.vue`

**删除内容**：

- 本地 `menuItems` 数组与图标导入
- `el-menu` 的 `router` 属性，避免与 `@select="handleMenuSelect"` 手动 push 重复触发导航

### 1.2 移除 NavTabs 中的左右翻页按钮与宽度过渡动画

**涉及文件**：`src/app/layouts/components/NavTabs.vue`

**删除内容**：

- 左右翻页按钮及相关 JS 逻辑、样式
- 基于 `flex-basis` / `max-width` 的 tab 宽度过渡动画，避免切换时相邻 tab 抖动

### 1.3 移除 DefaultLayout 中废弃的 stableTab 判断

**涉及文件**：`src/app/layouts/DefaultLayout.vue`

**删除内容**：

- `router-view` key 中残留的 `routeSlot.meta?.stableTab` 判断

---

## 二、新增/修改记录

### 2.1 新增菜单配置集中管理文件

**涉及文件**：`src/shared/constants/menu.ts`（**新增**）

**内容**：

- 集中定义侧边栏菜单结构、路径与图标
- 导出 `findMenuItemByPath` 函数，供标签栏根据路由匹配图标

### 2.2 标签栏状态支持图标与拖拽排序

**涉及文件**：`src/app/stores/tabs.ts`（**修改**）

**修改内容**：

- `NavTab` 接口新增 `icon?: Component` 字段
- `MAX_TABS` 从 12 调整为 7
- `addTab` 改为返回 `boolean`：已达上限且为新 tab 时返回 `false`
- 新增 `reorderTabs(newOrder)` 方法，支持拖拽后重新排序
- `readStorage` 中增加按 `path` 去重

### 2.3 标签栏 Hook 接入菜单图标与上限警告

**涉及文件**：`src/shared/composables/useTabs.ts`（**修改**）

**修改内容**：

- 新增 `resolveIcon(path)`：根据路由路径从菜单配置匹配图标
- 添加 tab 时传入 `icon`
- 达到上限时通过 `ElMessage.warning` 提示用户先关闭部分标签
- 增加 300ms 防抖，避免警告重复弹出
- 标签满时通过 `router.replace` 回退到上一个激活标签
- `/applications` 使用 `to.path` 作为 tab 键，内部模块切换不再新增标签

### 2.4 Sidebar 使用统一菜单配置

**涉及文件**：`src/app/layouts/components/Sidebar.vue`（**修改**）

**修改内容**：

- 从 `@/shared/constants/menu` 导入 `menuItems`
- 删除本地菜单定义和图标导入，保持侧边栏与标签栏图标来源一致

### 2.5 标签栏组件重构：Edge 风格、图标、拖拽

**涉及文件**：`src/app/layouts/components/NavTabs.vue`（**修改**）

**修改内容**：

- 使用 CSS Flex + CSS 变量 `--tab-base-width` / `--tab-min-width` 实现动态压缩
- 移除左右翻页按钮，改为横向滚动
- 修复关闭按钮默认隐藏但仍拦截点击的问题：`pointer-events: none` / hover 时 `auto`
- `activate` / `close` 中增加 `try/catch` 与 `isNavigationFailure` 错误处理
- 激活 tab 时自动 `scrollIntoView`
- 标签标题左侧显示页面图标；图标缺失时 fallback 到菜单配置
- 实现 HTML5 拖拽排序，拖拽中有半透明和放置目标高亮
- 固定标签（affix）不可拖动，也不允许被放置到其前面

### 2.6 ApplicationHub 模块切换即时响应

**涉及文件**：`src/features/applications/ApplicationHub.vue`（**修改**）

**修改内容**：

- `activeKey` 从 `computed` 改为 `ref` + `watch`，模块按钮点击立即切换本地状态
- 使用 `router.replace` 异步更新 URL，避免阻塞 UI
- 模块切换不再新增"个人档案信息申报"标签

### 2.7 个人中心子菜单补充图标

**涉及文件**：`src/shared/constants/menu.ts`（**修改**）

**修改内容**：

- "个人档案信息"添加 `IdCard` 图标
- "职业规划"添加 `Compass` 图标

### 2.8 修复 TypeScript 类型检查错误

**涉及文件**：`env.d.ts`、`package.json`、`package-lock.json`、`src/shared/types/types.ts`（**修改**）

**修改内容**：

- `env.d.ts` 添加 `export {}`，使文件成为 ES 模块，避免 `declare module 'vue-router'` 被当作模块声明覆盖真实导出
- `package.json` 将 `vue-router` 从 `^4.5.0` 固定为 `4.5.0`，避免 `4.6.4` 的 `.d.mts` 类型文件格式带来兼容性隐患
- `src/shared/types/types.ts` 补充 `SubmissionRecord` 和 `SubmissionFilters` 接口

---

## 三、影响范围

- **标签栏交互**：点击响应正常，关闭按钮不拦截 tab 点击；导航失败有日志反馈。
- **标签栏视觉**：多个标签时自动等比压缩，最低 80px；hover 显示完整标题；标签左侧显示对应页面图标。
- **标签数量**：最多保留 7 个标签，超出时阻止打开新页面并弹窗提示。
- **拖拽排序**：普通标签可拖动排序，固定标签始终钉在最左侧。
- **个人档案信息申报**：内部模块切换只保留一个标签，切换更流畅。
- **侧边栏导航**：菜单点击不再重复触发路由，标签上限警告只弹一次。
- **个人中心菜单**：子菜单项现在显示与功能主题匹配的图标。
- **类型检查**：`vue-tsc` 不再报 `vue-router` 导出缺失错误，`SubmissionRecord`/`SubmissionFilters` 类型已补齐。

---

## 四、验证命令

```bash
# 检查本次核心修改文件
npx eslint src/app/layouts/components/NavTabs.vue src/app/stores/tabs.ts src/shared/composables/useTabs.ts src/shared/constants/menu.ts src/app/layouts/components/Sidebar.vue src/app/layouts/DefaultLayout.vue src/features/applications/ApplicationHub.vue --max-warnings 0

# TypeScript 类型检查
npx vue-tsc --noEmit

# 生产构建
npx vite build
```

## 五、验证结果

- ESLint 检查：**通过，0 错误 / 0 警告**
- `npx vue-tsc --noEmit`：**通过，0 错误**
- `npx vite build`：**构建成功**

---

## 六、企业级改造：页面清理、架构扩展与消息中心

- **需求来源**：对企业级项目执行系统性改造，包括页面元素清理、架构可扩展性优化、消息中心功能开发，并遵循企业级应用规范。

### 6.1 删除记录

#### 6.1.1 移除全局面包屑

**涉及文件**：`src/app/layouts/DefaultLayout.vue`、`src/assets/styles/global.scss`

**删除内容**：

- `DefaultLayout.vue` 中的 `<nav class="layout__breadcrumb">` 面包屑导航区块
- `<script>` 中的 `breadcrumbs` computed、`goHome` 函数、`hasBreadcrumb` 相关逻辑
- `PageHeader` 默认插槽上的 `@click="goHome"` 事件
- `<style>` 中的 `&__breadcrumb { ... }` 样式
- `global.scss` 中 `.el-breadcrumb` 相关覆盖样式

**删除原因**：侧边栏 + Tab 栏已承担主导航职能，面包屑在首页会产生“首页 / 首页”重复展示，价值较低。

#### 6.1.2 移除个人档案信息申报相关 hero 卡片

**涉及文件**：`src/features/applications/ApplicationHub.vue`

**删除内容**：

- `<el-card class="application-hub__hero">` 及其内部 eyebrow / title / desc
- `.application-hub__hero`、`.application-hub__eyebrow`、`.application-hub__title`、`.application-hub__desc` 样式

**删除原因**：该卡片属于用户明确要求清理的“个人档案信息申报相关卡片组件”，移除后页面标题由 `DefaultLayout` 的 `route.meta.title` 统一承担。

#### 6.1.3 移除旧动态记录路由与目录

**涉及文件**：`src/app/router/routes.ts`

**删除内容**：原 `/activities` 独立路由。

**涉及目录**：`src/features/activities/`

**删除内容**：`ActivityList.vue` 已迁移至 `src/features/messages/activities/ActivityList.vue`，原目录删除。

### 6.2 新增/修改记录

#### 6.2.1 新增模块注册表

**涉及文件**：`src/shared/config/moduleRegistry.ts`（**新增**）

**内容**：

- 定义 `AppModule`、`MenuItem` 接口
- 在 `registeredModules` 中注册全部现有模块：`dashboard`、`growth-timeline`、`profile`、`messages`、`applications`、`awards`、`approval`
- 导出 `registeredModules`、`getMenuItems()`、`findModuleById()`
- 新增模块时，只需在该文件中添加一个 `AppModule` 条目，即可同步影响侧边栏菜单、模块图标与排序

#### 6.2.2 菜单配置改为注册表驱动

**涉及文件**：`src/shared/constants/menu.ts`（**修改**）

**修改内容**：

- 将硬编码的 `menuItems` 数组替换为 `getMenuItems()` 调用
- 保留并复用 `findMenuItemByPath` 函数

#### 6.2.3 新增消息中心类型

**涉及文件**：`src/shared/types/types.ts`（**修改**）

**新增内容**：

- `NotificationCategory`：`'system' | 'approval' | 'activity' | 'message'`
- `NotificationStatus`：`'read' | 'unread'`
- `Notification` 接口：包含 id、title、content、category、status、isRead、createdAt、link、sender
- `NotificationFilters` 接口：支持 category、status、keyword 筛选

#### 6.2.4 新增通知字典

**涉及文件**：`src/shared/constants/dict.ts`（**修改**）

**新增内容**：

- `NOTIFICATION_CATEGORY`：系统通知 / 审批提醒 / 动态提醒 / 私信
- `NOTIFICATION_STATUS`：已读 / 未读

#### 6.2.5 新增通知 Store

**涉及文件**：`src/app/stores/notification.ts`（**新增**）

**内容**：

- State：`notifications`、`filteredNotifications`、`loading`
- Computed：`unreadCount`
- Actions：`fetchNotifications(filters?)`、`markAsRead(id)`、`markAllAsRead()`、`deleteNotification(id)`
- 内置 8 条 Mock 数据覆盖 4 类分类与已读/未读状态
- 文件行数 156 行，符合 Store ≤200 行约定

#### 6.2.6 导出通知 Store

**涉及文件**：`src/app/stores/stores.ts`（**修改**）

**修改内容**：新增 `export { useNotificationStore } from './notification'`。

#### 6.2.7 新增消息中心主页面

**涉及文件**：`src/features/messages/MessageCenter.vue`（**新增**）

**内容**：

- 使用 `PageContainer`、`PageHeader`、`PageToolbar` 等现有共享组件
- 顶部统计卡片：未读消息数、消息总数、一键标为已读
- 分类筛选：全部 / 系统通知 / 审批提醒 / 动态提醒 / 私信
- 状态筛选：全部 / 未读 / 已读
- 通知列表：展示分类标签、标题、摘要、时间、已读状态点、“标为已读”/“删除”操作
- 底部提供“查看动态记录”入口，跳转 `/messages/activities`
- 文件行数 262 行，未超 600 行上限

#### 6.2.8 迁移动态记录页面

**源文件**：`src/features/activities/ActivityList.vue`

**目标文件**：`src/features/messages/activities/ActivityList.vue`

**内容**：保持原有 CRUD、搜索、分页、`approved` 只读逻辑不变；仅调整目录归属，纳入消息中心模块。

#### 6.2.9 注册消息中心路由

**涉及文件**：`src/app/router/routes.ts`（**修改**）

**新增内容**：

```ts
{
  path: 'messages',
  children: [
    {
      path: '',
      name: 'MessageCenter',
      component: () => import('@/features/messages/MessageCenter.vue'),
      meta: { title: '消息中心' },
    },
    {
      path: 'activities',
      name: 'MessageActivities',
      component: () => import('@/features/messages/activities/ActivityList.vue'),
      meta: { title: '全部动态' },
    },
  ],
}
```

同时删除原 `/activities` 路由。

#### 6.2.10 更新顶部铃铛图标

**涉及文件**：`src/app/layouts/components/HeaderBar.vue`（**修改**）

**修改内容**：

- 引入 `useNotificationStore`
- 铃铛按钮点击跳转 `/messages`
- `el-badge` 绑定 `notificationStore.unreadCount`，未读为 0 时隐藏
- 更新 `aria-label`，包含未读数量信息

#### 6.2.11 更新 Dashboard 查看全部链接

**涉及文件**：`src/features/dashboard/Dashboard.vue`（**修改**）

**修改内容**：将“最近动态”卡片的“查看全部”路由从 `/activities` 改为 `/messages/activities`。

#### 6.2.12 更新架构文档

**涉及文件**：`docs/architecture.md`（**修改**）

**修改内容**：

- 目录结构中补充 `src/shared/config/`
- Store 依赖表追加 `useNotificationStore`、`useActivityStore`
- 新增“模块注册表”小节，说明配置化菜单机制

#### 6.2.13 新增可扩展性设计文档

**涉及文件**：`docs/architecture/extensibility-design.md`（**新增**）

**内容**：

- 目标与范围
- 当前架构回顾（`features + shared + app` 分层）
- 扩展点清单（菜单、路由、Store、共享 UI、共享能力）
- 模块注册表契约（含 `AppModule` 接口）
- 新增模块标准操作流程（SOP）
- 配置化菜单实现说明
- 消息中心作为扩展示例
- 约束与红线
- 演进路线（权限感知菜单、模块级懒加载、微前端/插件化）

#### 6.2.14 验证期修复

**涉及文件**：

- `src/features/messages/MessageCenter.vue`：删除未使用的 `Notification` 类型导入，解决 ESLint `unused-imports/no-unused-imports` 报错
- `src/app/stores/notification.ts`：对 Mock 数据数组增加 `as Notification[]` 类型断言，解决 TypeScript `TS2322` 类型推断错误

### 6.3 影响范围

- **页面导航**：首页不再出现“首页 / 首页”面包屑；`ApplicationHub` 页面 hero 卡片已移除，模块 tabs 与 panel 正常显示。
- **侧边栏**：新增“消息中心”父菜单，含“通知”与“动态记录”两个子项，图标为 `Inbox` / `Bell` / `Clock`。
- **顶部栏**：铃铛图标点击跳转 `/messages`，未读消息数为 0 时不显示 badge。
- **首页 Dashboard**：“最近动态”模块的“查看全部”跳转 `/messages/activities`。
- **消息中心**：支持通知分类筛选、已读/未读筛选、一键已读、单条删除、跳转动态记录。
- **动态记录**：原 ActivityList 功能完整保留，路径变为 `/messages/activities`。
- **架构扩展性**：菜单由 `moduleRegistry.ts` 统一配置，新增模块只需修改注册表 + 路由 + feature 目录，无需多处维护菜单。

### 6.4 验证命令

```bash
# 全量 ESLint 检查
npx eslint . --max-warnings 0

# TypeScript 类型检查
npx vue-tsc --noEmit

# 生产构建
npx vite build
```

### 6.5 验证结果

- ESLint 检查：**通过，0 错误 / 0 警告**
- `npx vue-tsc --noEmit`：**通过，0 错误**
- `npx vite build`：**构建成功**

---

## 七、动态中心与消息中心细节优化

- **需求来源**：用户反馈「新增动态」入口未彻底移除、动态中心标题卡片与下方内容间距过近、搜索图标遮挡输入文字、表格/卡片逐行刷入动画效果不明显。

### 7.1 删除记录

#### 7.1.1 彻底移除「新增动态」能力

**涉及文件**：

- `src/app/stores/activity.ts`（**修改**）
- `src/features/messages/activities/ActivityList.vue`（**修改**）
- `src/features/messages/components/ActivityRecordDialog.vue`（**修改**）

**删除内容**：

- `useActivityStore` 中 `addActivity` 方法及其导出
- `ActivityList.vue` 中 `handleSave` 的新增分支（不再调用 `activityStore.addActivity`）
- `ActivityRecordDialog.vue` 中「新增动态」标题分支

**删除原因**：动态记录应由申报/审批行为自动生成，不允许用户手动发布。

### 7.2 新增/修改记录

#### 7.2.1 增大动态中心标题卡片与下方间距

**涉及文件**：`src/features/messages/activities/ActivityList.vue`（**修改**）

**修改内容**：`.center-panel` 的 `margin-top` 从 `20px` 调整为 `28px`。

#### 7.2.2 修复搜索图标遮挡输入文字

**涉及文件**：`src/features/messages/styles/_theme.scss`（**修改**）

**修改内容**：`mc-input-wrap` 混入中 `:deep(.el-input__wrapper)` 的 `padding-left` 从 `36px` 加大到 `42px`，确保搜索图标与输入文字之间保留充足间隙。

#### 7.2.3 重做表格逐行刷入动画

**涉及文件**：

- `src/assets/styles/variables.scss`（**修改**）
- `src/features/messages/activities/ActivityList.vue`（**修改**）

**修改内容**：

- 动画幅度加大：水平偏移从 `-24px/-36px` 统一调整为 `-60px`，并新增 `scale(0.96) → scale(1)` 的轻微缩放效果
- 动画时长从 `0.35s/0.5s` 统一调整为 `0.6s`，行间延迟保持 `0.08s`
- 删除 `@media (prefers-reduced-motion: reduce)` 兜底规则，确保动画默认可见
- 在 `variables.scss` 中新增可复用 `list-item-staggered` 混合宏，供卡片列表使用
- `ActivityList.vue` 中新增 `bumpActivityAnimation` / `bumpSubmissionAnimation` 方法，并在以下场景触发：
  - 数据加载完成（`watch(loading)`）
  - 标签切换（`watch(activeTab)`）
  - 分页切换（新增 `watch([pageNum, pageSize])`，`flush: 'post'`）

#### 7.2.4 消息中心卡片逐条刷入动画

**涉及文件**：`src/features/messages/MessageCenter.vue`（**修改**）

**修改内容**：

- 新增 `listKey` 及 `bumpListAnimation`，在 `notificationStore.loading` 变为 false 或分页变化时强制列表重渲染
- 通知列表容器新增 `message-list--staggered` 类
- 使用 `list-item-staggered` 混合宏实现卡片逐条从左侧滑入的「刷刷刷」效果

### 7.3 影响范围

- **动态记录**：不再支持手动新增动态，仅保留编辑与删除（已通过审核的仍不可删改）。
- **视觉间距**：动态中心标题卡片与下方工具栏/表格之间的留白更舒展。
- **搜索输入框**：动态记录与报名记录两个标签页的搜索框均不会因图标挤压文字。
- **加载动画**：动态记录表格、报名记录表格、消息中心通知卡片在加载、筛选、切换标签、切换分页时都会逐条/逐行从左侧滑入显示。

### 7.4 验证命令

```bash
# 检查本次修改文件
npx eslint src/features/messages/activities/ActivityList.vue src/features/messages/components/ActivityRecordDialog.vue src/features/messages/MessageCenter.vue src/app/stores/activity.ts --max-warnings 0

# TypeScript 类型检查
npx vue-tsc --noEmit

# 生产构建
npx vite build
```

### 7.5 验证结果

- ESLint 检查：**通过，0 错误 / 0 警告**
- `npx vue-tsc --noEmit`：**通过，0 错误**
- `npx vite build`：**构建成功**
- 浏览器验证：
  - 动态记录行 `animation` 计算属性为 `0.6s ... running table-row-brush-xxx`
  - 切到第 2 页后，第 2 行动画延迟为 `0.08s`，确认分页触发动画
  - 消息中心卡片同样存在逐条延迟动画
