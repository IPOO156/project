# 代码修改记录

- **日期**：2026-08-25
- **修改人**：nine-mirrors
- **需求来源**：按《学生端接口文档》（V5.8，2026-08-09，基础 URL `http://111.228.56.207:8089/api/v1`）全量接线——P0 五项 → P1 两项 → P2 API 层收敛 → 文档-代码全量比对后的 10 项缺口补全；Vite 代理目标地址调整。
- **验证结果**：
  - `node node_modules/eslint/bin/eslint.js src --max-warnings 0` 通过
  - `node node_modules/vue-tsc/bin/vue-tsc.js -b` 通过
  - 改动文件行数均 ≤600（不含注释空行放宽线 650 内）

---

## 一、删除记录

### 1.1 移除 submission.ts 遗留 mock 桩

**涉及文件**：`src/shared/api/submission.ts`

**删除内容**：5 个 mock 桩 `submitCorrection` / `getScoreIndicators` / `getEnrollmentInfo` / `checkDuplicate` / `pushNotification`，以及旧 mock 草稿桩 `saveDraft` / `loadDraft` / `deleteDraft`（文档无草稿列表/删除接口，本地兜底保留在 useFormDraft）。

**删除原因**：均已收敛到真实接口（见 2.x）；`pushNotification` 因后端 user_messages 自动生成通知，前端无需伪造。

### 1.2 移除 pushNotification 假调用

**涉及文件**：

- `src/app/stores/submission.ts`
- `src/shared/composables/useApplicationPage.ts`
- `src/shared/composables/useCorrection.ts`
- `src/features/teacher/material-review/composables/useReviewOperations.ts`

**删除内容**：共 7 处 `await pushNotification({ ... })` 调用，保留后续 `ElMessage.success` 提示。

### 1.3 移除申报/奖项列表页的 DuplicateCheckDialog 引用

**涉及文件**：15 个申报/奖项 `*List.vue`

**删除内容**：查重提示弹窗组件引用，改由 `ElMessageBox` 统一提示（见 2.x）。

### 1.4 简历导出移除本地生成主链路

**涉及文件**：`src/features/profile/composables/useResumeExport.ts`

**删除内容**：html2canvas + jspdf 本地生成不再作为主链路（仅保留为后端不可用时的回退分支）。

---

## 二、新增/修改记录

### 2.1 Vite 代理目标调整

**涉及文件**：`vite.config.ts`（**修改**）

**修改内容**：代理 target 指向 `http://111.228.56.207:8089`（文档 V5.8 基础 URL）；改后需重启 dev server 生效（见遗留事项）。

### 2.2 请求链路与 token 加固

**涉及文件**：

- `src/shared/utils/token.ts`（**修改**）
- `src/shared/api/request.ts`（**修改**）
- `src/app/router/routes.ts`（**修改**）

**修改内容**：token 链路加固；401 单飞 refresh token 刷新并回写新旧 token/refresh_token。

### 2.3 P0-草稿自动保存切真实接口

**涉及文件**：

- `src/shared/composables/useFormDraft.ts`（**修改**）
- `src/shared/composables/useApplicationPage.ts`（**修改**）
- `src/shared/api/submission.ts`（**修改**）

**修改内容**：草稿自动保存切 `autosaveApplication` / `autosaveAward`（PUT /applications/{id}/autosave、/awards/{id}/autosave）；首次建草稿走对应 submit 接口 `isDraft:1` 取回 archiveId/applicationId 回填表单。

### 2.4 P0-AI 对话切 §9 真实接口

**涉及文件**：

- `src/features/ai-chat/AIChat.vue`（**修改**）
- `src/features/ai-chat/components/AIChatDrawer.vue`（**修改**）
- `src/features/ai-chat/components/ChatMessages.vue`（**修改**）
- `src/features/ai-chat/components/ChatSidebar.vue`（**修改**）
- `src/features/ai-chat/components/MessageBubble.vue`（**修改**）
- `src/features/ai-chat/components/RichContentRenderer.vue`（**修改**）
- `src/features/ai-chat/composables/useAIChat.ts`（**修改**）
- `src/shared/api/ai-chat.ts`（**修改**）

**修改内容**：会话创建/列表/消息发送/重新生成/删除切真实接口；`conversationId` 统一为 `number`；网络层错误离线回退本地 mock 并标注，业务错误真实气泡。

### 2.5 P0-成长时间轴切 §4.2 真实接口

**涉及文件**：

- `src/features/growth-timeline/GrowthTimeline.vue`（**修改**）
- `src/features/growth-timeline/components/ExperienceDetail.vue`（**修改**）
- `src/features/growth-timeline/components/GrowthCard.vue`（**修改**）
- `src/features/growth-timeline/composables/useGrowthTimeline.ts`（**修改**）
- `src/features/growth-timeline/timeline-constants.ts`（**修改**）

**修改内容**：接入 `GET /profile/growth-timeline` 真实接口；事件类型 1-6 整数枚举对齐；`statusLabel` 展示；本地数据合并。

### 2.6 P0-职业规划 semesterId 映射与 5 态

**涉及文件**：

- `src/shared/api/career-plan.ts`（**修改**）
- `src/app/stores/career-plan.ts`（**修改**）
- `src/features/profile/CareerPlan.vue`（**修改**）

**修改内容**：提交前经 `getSemesters()` 按业务学期名（`2023-2024-1`）匹配数字 `semesterId`，禁止 `Number()` 强制转换；状态五态对齐（0 草稿/1 待审核/2 已通过/3 已退回/4 已撤销）；展示 `progressRate` / `statusLabel`；修复 13 处类型错误。

### 2.7 P0-兴趣标签切真实接口

**涉及文件**：

- `src/shared/api/archive.ts`（**修改**）
- `src/app/stores/archive.ts`（**修改**）
- `src/features/profile/ProfileInfo.vue`（**修改**）
- `src/shared/types/types.ts`（**修改**）
- `src/shared/constants/dict.ts`（**修改**）

**修改内容**：兴趣标签改走 `PUT /profile/interests` / `DELETE /profile/interests/{id}`；字段对齐 `tagName` / `detailContent` / `proficiencyLevel`(1-4) / `isDetail`。

### 2.8 P1-职业规划详情/目标/行动/里程碑

**涉及文件**：

- `src/shared/api/career-plan.ts`（**修改**）——补 `updateCareerMilestone` / `deleteCareerMilestone`（§4.10.1/§4.10.2）
- `src/features/profile/CareerPlan.vue`（**修改**）——列表加「详情」入口
- `src/features/profile/components/CareerPlanDetail.vue`（**新增**）——详情抽屉（目标/行动/里程碑三级结构）
- `src/features/profile/components/CareerPlanInsights.vue`（**新增**）——阶段反思/教师反馈/版本历史 + 新增反思（§4.13）
- `src/features/profile/components/CareerPlanEditDialog.vue`（**新增**）——目标/行动/里程碑编辑弹窗
- `src/features/profile/components/CareerPlanActionUpload.vue`（**新增**）——行动成果上传（§4.12.1）
- `src/features/profile/components/CopyCareerPlanDialog.vue`（**新增**）——复制上一学期计划（§4.7）

**修改内容**：目标/行动/里程碑增删改全链路；里程碑达成开关唯一写入入口为 §4.10.1；仅草稿/已退回（status=0/3）可编辑，已通过只读；复制上一学期计划经 `getSemesters()` 数字 ID 映射。

### 2.9 P1-简历导出切后端

**涉及文件**：

- `src/features/profile/composables/useResumeExport.ts`（**修改**）
- `src/features/profile/ResumeTemplate.vue`（**修改**）

**修改内容**：导出主链路切 §4.18/§4.19（`GET /profile/resume/export/preview` → `POST /profile/resume/export`）；栏目 code 对齐 `practices` / `selfEvaluation`（原 experiences/evaluation）；弹窗增加 `internal`/`external` 用途选择；成功展示 `expireAt` 过期时间并 `window.open(downloadUrl)`；后端不可用回退本地生成。

### 2.10 P2-API 层收敛（查重/纠错）

**涉及文件**：`src/shared/composables/useApplicationPage.ts`（**修改**）

**修改内容**：查重切真实 `POST /applications/duplicate-check`（补 `archiveType` / `title` / `obtainedTime` 传参），重复时 `ElMessageBox` 展示相似记录与建议；纠错沿用真实 `correctApplication`。

### 2.11 缺口#1/#10-评分计算说明与指标体系树

**涉及文件**：

- `src/shared/ui/ScoreIndicatorDialog.vue`（**修改**）
- `src/shared/composables/useScoreIndicator.ts`（**修改**）
- `src/features/profile/components/DimensionPanel.vue`（**修改**）
- `src/features/dashboard/Dashboard.vue`（**修改**）

**修改内容**：评分明细逐维度「计算说明」（§4.1.3，`GET /profile/scores/{calculationId}/details`：规则版本/数据来源/计算时间/加权明细/依据档案 el-tag）；新增「指标体系」tab（§3.7 `GET /common/indicators` + el-tree 渲染，含 versionName/effectiveAt）。

### 2.12 缺口#7-学生端档案导出

**涉及文件**：

- `src/features/profile/composables/useArchiveExport.ts`（**新增**）
- `src/features/profile/ProfileInfo.vue`（**修改**）
- `src/features/profile/components/InterestPanel.vue`（**新增**）

**内容**：`exportArchivePDF()` 先 §4.16 preview 拉栏目（失败回退默认栏目），弹窗勾选 + `internal`/`external` 用途，确认后 §4.17 submit 取 `downloadUrl` 下载并提示 `expireAt`；`ProfileInfo` 头部加「导出档案」按钮；兴趣面板抽取以控制行数。

### 2.13 缺口#8-评选说明入口

**涉及文件**：`src/shared/ui/ApplicationFormRecord.vue`（**修改**）

**修改内容**：操作列「评选说明」入口，按 archive/award 区分调 `getApplicationGuide`（§7.3）/ `getAwardGuide`（§8.3），展示 title/typeLabel、content、requirements 必填表、notes、updatedAt；未改变该共享组件的 props/emits 契约。

### 2.14 缺口#9-学生端消息设置

**涉及文件**：

- `src/features/messages/MessageCenter.vue`（**修改**）
- `src/features/messages/components/MessageSettings.vue`（**新增**）
- `src/shared/api/notification.ts`（**修改**）

**内容**：头部「消息设置」按钮 → `MessageSettings` 面板（邮件/短信/站内推送三路开关，§5.8/§5.9）；`updateMessageSettings` 移除 `.catch(() => undefined)` 让失败透传请求拦截器统一提示。

---

## 三、影响范围

- **接口接线**：《学生端接口文档》100 个端点中 98 个 API 函数 + 页面调用就绪；教师端 `admin/*` 不在学生端文档范围。
- **登录/请求链路**：token 刷新、401 处理加固；代理地址已改（见遗留事项）。
- **职业规划/评分/简历/消息**：详情三级结构、计算说明、指标树、档案导出、评选说明、消息设置等均落地。
- **行数约束**：新增/修改文件均 ≤600 行；`Dashboard.vue` 恰好 600 行到上限。

---

## 四、验证命令

```bash
node node_modules/eslint/bin/eslint.js src --max-warnings 0
node node_modules/vue-tsc/bin/vue-tsc.js -b
```

## 五、验证结果

- ESLint 检查：**通过，0 错误 / 0 警告**
- `vue-tsc -b`：**通过，0 错误**
- 改动文件行数均 ≤600（不含注释空行放宽线 650 内）

## 六、遗留事项

1. **缺口#6-AI 建议一键添加计划（§4.14）**：`aiAddCareerPlan` API 已就绪；AI 分析为前端本地生成、后端未下发 `aiSuggestionId`，代码保留 TODO 待后端字段就绪后接线。
2. **§4.5 规划文件下载契约**：`downloadCareerPlanFile` 按 `downloadUrl` 读取下载，若后端实际为重定向需联调确认。
3. **Vite 代理**：按文档指向 `111.228.56.207:8089`；需确认该地址本机可达，改动后必须重启 dev server。
4. **`DuplicateCheckDialog.vue` 组件文件已无引用未删除**（`src/shared/ui/index.ts` 仍有一处 barrel 导出，无害）。
