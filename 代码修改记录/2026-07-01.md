# 代码修改记录

- **日期**：2026-07-01
- **修改人**：nine-mirrors
- **需求来源**：按 `claude.md`、`开发手册.md`、`前端开发规范与最佳实践（优化版）.md` 检查并整改当前前端项目
- **验证结果**：
  - `npx eslint --max-warnings 0 .` 通过
  - `npx vue-tsc --noEmit --project tsconfig.json` 通过
  - `npm run build` 通过

---

## 一、删除记录

### 1.1 移除 List 页面表格中的内联 `el-tag` 状态写法

**涉及文件**：
`src/features/applications/*/pages/*List.vue`、`src/features/approval/pages/SubmitRecords.vue`

**删除示例**（以 `CompetitionList.vue` 为例）：

```vue
<!-- 删除前 -->
<el-table-column label="状态" width="100">
  <template #default="{ row }">
    <el-tag v-if="row.status === 'approved'" type="success" size="small">已通过</el-tag>
    <el-tag v-else-if="row.status === 'submitted'" type="warning" size="small">已提交</el-tag>
    <el-tag v-else-if="row.status === 'rejected'" type="danger" size="small">已驳回</el-tag>
    <el-tag v-else type="info" size="small">草稿</el-tag>
  </template>
</el-table-column>
```

### 1.2 移除 List 页面中的硬编码内联样式

**涉及文件**：
`src/features/applications/*/pages/*List.vue`

**删除示例**：

```vue
<!-- 删除前 -->
<div class="app-page__actions" style="margin-top: 16px;">
  <el-button type="primary" :icon="Plus" @click="app.openCreate()">新增申报</el-button>
</div>

<el-card style="margin-top: 16px;">
  ...
</el-card>

<el-select v-model="app.formData.semester" placeholder="请选择" style="width: 200px">
  ...
</el-select>
```

### 1.3 移除 `SubmitRecords.vue` 中冗余的状态标签处理逻辑

**涉及文件**：`src/features/approval/pages/SubmitRecords.vue`

**删除代码**：

```ts
import type { TagProps } from 'element-plus'
import { useDict } from '@/shared/composables'
import { APPLICATION_STATUS } from '@/shared/constants/dict'

const { getColor, getLabel } = useDict(APPLICATION_STATUS)

function getStatusType(status: string): TagProps['type'] {
  return (getColor(status) as TagProps['type']) ?? 'info'
}
```

```vue
<el-tag :type="getStatusType(row.status)" size="small">
  {{ getLabel(row.status) }}
</el-tag>
```

### 1.4 移除 `useFormValidate.ts` 中错误的 `FormRules` 返回类型

**涉及文件**：`src/shared/composables/useFormValidate.ts`

**删除代码**：

```ts
import type { FormInstance, FormRules } from 'element-plus'

export function requiredRule(message: string): FormRules {
  return { required: true, message, trigger: 'blur' }
}
```

### 1.5 移除 `tsconfig.node.json` 中冲突的 `allowImportingTsExtensions`

**涉及文件**：`tsconfig.node.json`

**删除配置**：

```json
"allowImportingTsExtensions": true,
```

### 1.6 移除 `docs/change-logs/` 目录

**涉及文件**：`docs/change-logs/2026-07-01.md`

**说明**：根据《开发手册》代码修改记录规范，修改记录统一存放于项目根目录 `代码修改记录/` 下，原 `docs/change-logs/` 不再使用，已删除。

---

## 二、新增记录

### 2.1 工程配置：新增 husky + lint-staged

**涉及文件**：`package.json`、`.husky/pre-commit`

**新增 `package.json` 配置**：

```json
{
  "scripts": {
    "lint": "eslint . --fix",
    "lint:check": "eslint . --max-warnings 0",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3"
  },
  "lint-staged": {
    "*.{js,ts,vue}": ["eslint --fix", "prettier --write"],
    "*.{scss,css,json,md}": ["prettier --write"]
  }
}
```

**新增 `.husky/pre-commit`**：

```sh
npx lint-staged
```

### 2.2 ESLint 配置：新增参考手册、文档与修改记录忽略规则

**涉及文件**：`eslint.config.js`

```js
ignores: [
  'claude.md',
  '开发手册.md',
  '前端开发规范与最佳实践（优化版）.md',
  'docs/**/*.md',
  '代码修改记录/**/*.md',
  '.husky/**',
]
```

### 2.3 Git 忽略：新增构建产物与缓存文件

**涉及文件**：`.gitignore`

```gitignore
# TypeScript composite project emit artifacts
vite.config.js
vite.config.d.ts
*.tsbuildinfo
```

### 2.4 新增通用组件 `StatusTag`

**涉及文件**：`src/shared/ui/StatusTag.vue`、`docs/components.md`

**新增组件代码**：

```vue
<script setup lang="ts">
import type { TagProps } from 'element-plus'
import { computed } from 'vue'
import { APPLICATION_STATUS } from '@/shared/constants/dict'
import { useDict } from '@/shared/composables'

interface Props {
  status: string
  size?: TagProps['size']
}

const props = withDefaults(defineProps<Props>(), {
  size: 'small',
})

const { getColor, getLabel } = useDict(APPLICATION_STATUS)

const tagType = computed<TagProps['type']>(() => {
  return (getColor(props.status) as TagProps['type']) ?? 'info'
})
</script>

<template>
  <el-tag :type="tagType" :size="size">
    {{ getLabel(status) }}
  </el-tag>
</template>
```

### 2.5 List 页面统一使用 `StatusTag` 与类名化样式

**涉及文件**：
`src/features/applications/book-report/pages/BookReportList.vue`
`src/features/applications/certificate/pages/CertificateList.vue`
`src/features/applications/competition/pages/CompetitionList.vue`
`src/features/applications/competition-star/pages/CompetitionStarList.vue`
`src/features/applications/innovation/pages/InnovationList.vue`
`src/features/applications/internship/pages/InternshipList.vue`
`src/features/applications/organization/pages/OrganizationList.vue`
`src/features/applications/research/pages/ResearchList.vue`
`src/features/applications/scholarship/pages/ScholarshipList.vue`
`src/features/applications/scientific-project/pages/ScientificProjectList.vue`
`src/features/applications/scientific-star/pages/ScientificStarList.vue`
`src/features/applications/social-practice/pages/SocialPracticeList.vue`
`src/features/applications/training/pages/TrainingList.vue`
`src/features/approval/pages/SubmitRecords.vue`

**新增示例**（以 `CompetitionList.vue` 为例）：

```vue
<script setup lang="ts">
import StatusTag from '@/shared/ui/StatusTag.vue'
</script>

<template>
  <div class="app-page__actions mt-16">
    <el-button type="primary" :icon="Plus" @click="app.openCreate()">新增申报</el-button>
  </div>

  <el-card class="mt-16">
    <el-table :data="list" stripe>
      ...
      <el-table-column label="状态" width="100">
        <template #default="{ row }">
          <StatusTag :status="row.status" size="small" />
        </template>
      </el-table-column>
    </el-table>
  </el-card>
</template>
```

```vue
<el-select v-model="app.formData.semester" placeholder="请选择" class="form-select">
  <el-option
    v-for="s in SEMESTER_OPTIONS"
    :key="s.value"
    :label="s.label"
    :value="s.value"
  />
</el-select>
```

### 2.6 新增通用工具类

**涉及文件**：`src/assets/styles/global.scss`

```scss
.mt-16 { margin-top: 16px; }
.mb-8 { margin-bottom: 8px; }
.form-select { width: 200px; }
.form-select--sm { width: 160px; }
.form-input { width: 200px; }
```

### 2.7 Mock 数据统一标注

**涉及文件**：所有 `*List.vue`

```ts
// Mock 数据（接口联调后替换）
const list = ref<CompetitionItem[]>([
  { id: '1', name: '...', status: 'approved' },
  ...
])
```

### 2.8 字典常量扩展

**涉及文件**：`src/shared/constants/dict.ts`

```ts
export const APPROVAL_STATUS_OPTIONS = [
  { label: '已通过', value: 'approved' },
  { label: '已驳回', value: 'rejected' },
  { label: '待审批', value: 'pending' },
] as const

export const TIMELINE_EVENT_TYPES = {
  award: { label: '奖项', color: '#e6a23c', iconKey: 'Award' },
  practice: { label: '实践', color: '#67c23a', iconKey: 'Users' },
  grade: { label: '成绩', color: '#409eff', iconKey: 'BookOpen' },
  competition: { label: '竞赛', color: '#9b59b6', iconKey: 'Code' },
  internship: { label: '实习', color: '#f56c6c', iconKey: 'Briefcase' },
  other: { label: '其他', color: '#909399', iconKey: 'Star' },
} as const

export const INTEREST_LEVEL = {
  proficient: { label: '熟练', color: 'success' },
  good: { label: '良好', color: 'warning' },
  general: { label: '一般', color: 'info' },
} as const

export const ACTIVITY_STATUS = {
  submitted: { label: '已提交', color: '#e6a23c' },
  approved: { label: '已通过', color: '#67c23a' },
  rejected: { label: '需修改', color: '#f56c6c' },
} as const
```

### 2.9 修复 `SubmitRecords.vue` 类型错误

**涉及文件**：`src/features/approval/pages/SubmitRecords.vue`

```vue
<template>
  <el-table-column label="操作" width="120" fixed="right">
    <template #default="{ row }">
      <el-button
        text
        type="primary"
        :icon="Eye"
        size="small"
        @click="viewDetail(row as SubmitRecord)"
      >
        查看
      </el-button>
    </template>
  </el-table-column>
</template>
```

### 2.10 修复 `useFormValidate.ts` 类型定义

**涉及文件**：`src/shared/composables/useFormValidate.ts`

```ts
import type { FormInstance, FormItemRule } from 'element-plus'

export function requiredRule(message: string): FormItemRule {
  return { required: true, message, trigger: 'blur' }
}
```

### 2.11 新增项目文档

**涉及文件**：
- `docs/architecture.md`
- `docs/api.md`
- `docs/components.md`
- `docs/COMPOSABLES_GUIDE.md`
- `docs/style-guide.md`
- `代码修改记录/2026-07-01.md`
- `CONTRIBUTING.md`
- `README.md`
- `.husky/pre-commit`

**新增通用 Composables**：
- `src/shared/composables/useExport.ts`
- `src/shared/composables/useFormValidate.ts`
- `src/shared/composables/usePermission.ts`

---

## 三、补充修正（记录目录整理与审计后修复）

### 3.1 调整代码修改记录存放位置

**涉及文件**：`docs/change-logs/2026-07-01.md` → `代码修改记录/2026-07-01.md`

**说明**：按《开发手册》代码修改记录规范，修改记录统一存放于项目根目录 `代码修改记录/` 下，删除原 `docs/change-logs/` 目录，并同步更新 `eslint.config.js` 忽略规则。

### 3.2 新增 README.md

**涉及文件**：`README.md`

**说明**：补充项目说明、技术栈、目录结构、开发规范、常用脚本、代理配置等内容；提交信息格式与三本手册保持一致（`YYYY/MM/DD-方向-操作内容`）。

### 3.3 修复剩余内联样式与本地字典

**涉及文件**：
- `src/features/applications/social-practice/pages/SocialPracticeList.vue`：将 `el-input-number` 的 `style="width: 200px"` 改为 `class="form-input-number"`
- `src/features/applications/scholarship/pages/ScholarshipList.vue`：删除本地 `gradeMap`，统一使用 `SCHOLARSHIP_GRADES`
- `src/features/applications/innovation/pages/InnovationList.vue`：删除本地 `companyTypes`，统一使用 `INNOVATION_COMPANY_TYPES`
- `src/shared/constants/dict.ts`：新增 `SCHOLARSHIP_GRADES`、`INNOVATION_COMPANY_TYPES`
- `src/assets/styles/global.scss`：新增 `.form-input-number` 工具类

### 3.4 移除模板中的类型断言

**涉及文件**：`src/features/approval/pages/SubmitRecords.vue`

**说明**：将模板里的 `(row as SubmitRecord)` 移到脚本层 `statusOf(row)` 与 `viewDetailByRow(row)` 做安全断言，模板保持简洁。

### 3.5 拍平 `src/features/**/pages/` 单文件目录

**涉及文件**：
- `src/features/applications/*/pages/*List.vue` → `src/features/applications/*/*List.vue`
- `src/features/approval/pages/*.vue` → `src/features/approval/*.vue`
- `src/features/auth/pages/Login.vue` → `src/features/auth/Login.vue`
- `src/features/dashboard/pages/Dashboard.vue` → `src/features/dashboard/Dashboard.vue`
- `src/features/profile/pages/*.vue` → `src/features/profile/*.vue`
- `src/app/router/index.ts`

**说明**：每个 `pages` 目录下仅含 1-2 个文件，属于多余嵌套；拍平后最深路径从 5 层降到 4 层，符合《前端开发规范与最佳实践（优化版）》“路径不要超过 4 层”的要求。同步更新路由懒加载路径。

---

## 四、验证命令

```bash
npx eslint --max-warnings 0 .
npx vue-tsc --noEmit --project tsconfig.json
npm run build
```
