# 代码修改记录 2026-09-23

- **修改人**：ry
- **修改日期**：2026-09-23
- **需求来源**：全面检测学生端 / 教师端 / 管理员端接口对接情况，清理假数据与死代码，修复接口接错问题
- **关联报告**：`docs/2026-09-23-三端接口与假数据审计报告.md`
- **变更统计**：14 个文件修改，删除 3 个文件，新增 1 个文件（+83 / -1457 行）

---

## 一、删除范围

### 1.1 整文件删除（3 个）

| 文件                                                     | 原行数 | 删除原因                                                                                                                                              |
| -------------------------------------------------------- | :----: | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/features/approval/composables/useStarMockData.ts`   |  244   | `useStarMockData()` 生成 8 条「张三」编造之星报名记录，全项目零引用；文件内其余两个纯函数（`filterStarRecords` / `canEditStar`）与类型已迁至新文件    |
| `src/features/approval/composables/useReviewMockData.ts` |  438   | 整套评审 Mock 数据生成器，`useReviewMockData()` / `useAllReviewMockData()` 全项目零引用；`ReviewRecord` 类型实际来自 `@/shared/types/types`，非本文件 |
| `src/features/teacher-shared/StudentSelector.vue`        |  240   | 死组件：无 import、无模板使用、未在 `components.d.ts` 自动注册；其 `classOptions` 用 `` `${major}1班` `` 拼造班级名                                   |

### 1.2 文件内删除

| 文件                                                 | 删除内容                                                                                                                                                                                                                                                                                                                                              |
| ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/features/growth-timeline/timeline-constants.ts` | `INITIAL_EXPERIENCES`（5 条凭空编造的成长经历，含「第一粒种子」「图书馆的冬天与夏天」等）。该常量在首次渲染即展示，且接口失败后永久保留                                                                                                                                                                                                               |
| `src/shared/api/archive.ts`                          | `idCounter`、`nextId()`、`getGrades()`、`getAwards()`、`addAward()`、`updateAward()`、`deleteAward()`。前两者为 `setTimeout` + 硬编码「高等数学/线性代数/数据结构/操作系统」与「全国大学生数学建模竞赛」等；被 `app/stores/archive.ts` 的异常兜底分支调用，即 **`GET /profile/info` 失败时用户会看到编造成绩与奖项**                                  |
| `src/shared/api/archive.ts`                          | 类型导入中的 `Award`、`Grade`（随上述函数一并移除）                                                                                                                                                                                                                                                                                                   |
| `src/shared/api/activity.ts`                         | `generateMockActivities()`、`MOCK_ACTIVITIES`（14 条编造动态）、`updateActivity()`、`deleteActivity()`。后两者路径形态错误（`PUT /activities/{id}?type=archive`，后端契约是 `PUT /activities/{type}/{id}`，type 为路径变量），必然 404 且被 `.catch(() => undefined)` 吞掉                                                                            |
| `src/shared/api/activity.ts`                         | `getActivities()` 的 `.catch()` Mock 兜底分支，改为返回空数组                                                                                                                                                                                                                                                                                         |
| `src/shared/api/applications.ts`                     | `ApplicationCommonFields`、`SubmitApplicationResult` 两个接口；`submitApplication(type, payload)` 与 11 个 `submitXxx` 包装（practice / bookReview / competition / scholarship / innovation / research / certificate / internship / organization / training）。**与 `submission.ts` 的同名 `submitApplication` 重名**，且不做字段归一化，全项目零引用 |
| `src/app/stores/activity.ts`                         | `updateActivity()`、`deleteActivity()` 两个 action，以及 `deleteActivity as apiDelete` / `updateActivity as apiUpdate` 导入与 `ElMessage` 导入。两个 action 全项目零调用，且依赖已删除的错误路径 API                                                                                                                                                  |
| `src/app/stores/archive.ts`                          | `fetchArchiveMock()`，由 `fetchArchiveFallback()` 取代（见新增部分）                                                                                                                                                                                                                                                                                  |

---

## 二、新增范围

### 2.1 新增文件（1 个）

| 文件                                               | 内容                                                                                                                                  |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `src/features/approval/composables/starRecords.ts` | 从 `useStarMockData.ts` 迁入，仅保留无 mock 的部分：`StarRecord` 接口、`SCIENTIFIC_SUB_TYPES`、`filterStarRecords()`、`canEditStar()` |

### 2.2 文件内新增 / 修改

| 文件                                                            | 内容                                                                                                                                                                                                  |
| --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/shared/types/teacher.ts`                                   | 新增 `TeacherDelegationCandidate`、`TeacherDelegationCandidateResult` 接口（字段对齐后端 `DelegationCandidateItem` / `DelegationCandidateResponse`）                                                  |
| `src/shared/api/teacher.ts`                                     | 新增 `listTeacherDelegationCandidates({ keyword?, collegeId? })`，对接 `GET /teacher/delegations/candidates`                                                                                          |
| `src/features/teacher/delegation/DelegationManage.vue`          | **P0 修复**：`loadDelegatees()` 由 `listUsers({ roleId: 3, ... })`（即 `GET /admin/users`，教师身份调用必然 403）改为 `listTeacherDelegationCandidates()`。此前下拉框永久为空，新建委托功能完全不可用 |
| `src/features/growth-timeline/composables/useGrowthTimeline.ts` | `experiences` 初值由 `INITIAL_EXPERIENCES` 改为 `[]`；`rebuildExperiences()` / `loadBackendTimeline()` 的后端失败分支改为不注入示例数据，展示空态                                                     |
| `src/app/stores/archive.ts`                                     | `fetchArchiveMock()` → `fetchArchiveFallback()`：`/profile/info` 失败时只清空 `grades` / `awards` 展示空态，维度画像仍走独立真实接口 `GET /archive/dimensions` 兜底；同步更新 Store 顶部注释          |
| `src/features/approval/components/StarSection.vue`              | 导入路径由 `../composables/useStarMockData` 改为 `../composables/starRecords`                                                                                                                         |

---

## 三、验证

- `npx vue-tsc -b --force`：**通过，0 报错**
- `npx eslint --max-warnings 0`（全部改动文件）：**通过，0 报错 0 警告**
- 接口比对脚本复跑：前端"有调用无匹配"由 6 条降至 1 条，且剩余 1 条经核实为通配抽象误报（`POST /awards/research-star/{id}/{projects|software|papers}` 与后端 3 个具体端点等价），实际为 **0 条**
