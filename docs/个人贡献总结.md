# 个人贡献总结（面试详版）—— 学生端档案管理系统（前端）

> Git 身份：`nine-mirrors <2182018986@qq.com>` ｜ 贡献期：2026-07-01 ~ 2026-09-07
> 本文档按「介绍口径 → 架构 → 深度解析 → 追问应对」组织，可直接用于考察/面试陈述。

---

## 一、电梯陈述（背这三段就够）

### 30 秒版

> 我在学生端档案管理系统（Vue 3 + TS + Pinia + Element Plus）中承担核心前端开发：团队 4 人中提交量第一（62 次提交、约占 44%，累计 +67,000 行）。我**从 0 到 1 构建了成长时间轴可视化模块**（21 个文件 / 约 4700 行），**重构了 13 个申报/奖项业务模块**并抽取公共表单逻辑，主导了**学生端接口 V5.8 全量对接**，并修复了**登录态 401 刷新死循环**这一生产级稳定性问题。

### 1 分钟版（加两条技术深度）

> 在此基础上：请求层我实现了 **401 令牌刷新的"单飞"并发去重 + 3 次重试上限 + 业务 401 分流**三层防护，消除了退出登录死循环；架构上我抽取了 `useApplicationForm` **泛型组合式函数**，收敛 10 个申报模块的重复表单逻辑；工程化上我搭建了 **CI 三层门禁**（ESLint 零警告 → vue-tsc 类型检查 → 构建）并制定了 450 行的 AI 协作开发规范。

### 3 分钟版（考察开场白，按时间线讲）

> **第一阶段（7 月初）做工程化**：项目刚起步时先立规矩——我按团队三本手册完成目录结构整改（72 个文件）、搭建 GitHub Actions CI 门禁、修复 husky 在 GUI 客户端的 PATH 问题，建立了「代码修改记录」制度，让 4 人协作有统一的规范底座。
>
> **第二阶段（7 月上旬）做核心业务**：我独立设计实现了「成长时间轴」模块——用原子轨道主视觉 + 滚动显现动画呈现学生的成长轨迹，内部拆了 17 个组件、4 个 composables；随后重构了 10 个申报 + 3 个奖项模块，把重复的表单校验/提交/重置逻辑抽成泛型 `useApplicationForm`；还建设了消息与动态中心、AI 助手的 UI 体系。
>
> **第三阶段（8 月下旬）做接口对接**：对照接口文档 V5.8 完成 29 个文件的全量接线，把 10 个 Store 从 Mock 切到真实 API，补齐了 10 项文档有但代码缺的接口，统一了服务端分页类型。
>
> **第四阶段（9 月）做稳定性治理**：定位并修复了退出登录 401 死循环——刷新单飞、重试上限、业务 401 分流、匿名 401 下抛四层防护；同时给所有 Store 加了「并发去重 + TTL 缓存」双层请求治理。

---

## 二、项目背景与技术架构

### 2.1 项目是什么

面向高校学生的**综合成长档案管理平台**：学生申报实习/竞赛/科研等 10 类经历与 3 类评奖，教师端审核，系统沉淀为成长档案、生成可视化时间轴与能力画像，并提供 AI 助手与职业规划。团队 4 名前端分工：我负责**学生端核心业务 + 工程化 + 稳定性**，另两位同事分别负责教师端、管理员端，一人负责登录与个人中心。

### 2.2 技术栈（含版本，可直接报）

| 类别     | 选型                                                    |
| -------- | ------------------------------------------------------- |
| 框架     | Vue **3.5**（Composition API + `<script setup>`）       |
| 状态管理 | Pinia **3.0**（全部为 Setup Store 风格）                |
| UI 库    | Element Plus **2.9**                                    |
| 类型     | TypeScript **5.7**（`vue-tsc` 严格检查）                |
| 构建     | Vite **6.2**（dev 代理 `/api/v1` → 后端）               |
| 规范     | ESLint 9（@antfu 配置）+ Prettier + husky + lint-staged |
| CI       | GitHub Actions（Node 22）                               |

### 2.3 分层架构（我参与落地的目录规范）

```
src/
├── app/            # 入口层：路由 routes.ts、布局、Pinia Store（14 个）
├── features/       # 业务模块层：applications/awards/growth-timeline/
│                   #   messages/ai-chat/dashboard/profile/approval...
├── shared/         # 共享层：api/（16 个接口模块）、composables/（18 个）、
│                   #   types/、constants/（字典集中维护）、ui/、utils/
```

关键约束（写进了团队规范）：**禁止跨层引用**、feature 之间只能通过公共 Store 通信、字典集中在 `constants/dict.ts`、单文件 ≤600 行。

---

## 三、贡献数据总览

| 指标          | 数据                                                                                                         |
| ------------- | ------------------------------------------------------------------------------------------------------------ |
| 提交次数      | **62 次**（4 人中第 1，约占 44%；第二名 43 次）                                                              |
| 代码量        | 非合并提交累计 **+67,425 / -22,074 行**，901 次文件改动                                                      |
| 最大单次      | Logo 更换+组件拆分+冗余清理专项：116 文件 +12,635 行                                                         |
| 原创/主导模块 | 成长时间轴（约 4,700 行）、消息中心（约 1,800 行）、AI 助手（约 4,800 行，10 次提交中 7 次）、CI（唯一作者） |
| 深度重构      | 10 个申报 + 3 个奖项模块（每个核心文件 12 次迭代）、`useApplicationForm`（唯一作者，4/4 提交）               |

---

## 四、核心贡献深度解析（考察重点）

### 4.1 【稳定性】401 令牌刷新治理 —— 最能体现深度的一项

**代码位置**：`src/shared/api/request.ts`

#### 背景（Bug 现象）

用户点「退出登录」后页面以约 150ms 一次的频率无限 reload；登录页输错密码时会把已登录用户误登出。根因是**把所有 401 都当成"令牌过期"处理**，而实际上 401 有四种语义。

#### 方案：四层防护

**① 刷新单飞（Singleton）—— 解决并发重复刷新**

```typescript
let refreshPromise: Promise<string | null> | null = null

function tryRefreshToken(): Promise<string | null> {
  const refreshToken = getRefreshToken()
  if (!refreshToken) return Promise.resolve(null)
  if (!refreshPromise) {
    // 第一个 401 发起刷新
    refreshPromise = request
      .post('/auth/refresh', { refreshToken })
      .then(/* 回写新令牌 */)
      .finally(() => {
        refreshPromise = null
      })
  }
  return refreshPromise // 并发的其他 401 复用同一个 Promise
}
```

> **为什么单飞**：页面加载时十几个请求同时 401，若各自刷新，第一个成功后 `refreshToken` 已轮换（后端一次性），后续刷新全部失败 → 全部被登出。单飞保证同一时刻只有一个刷新请求。

**② 重试上限 —— 解决无限循环**

```typescript
if (((error.config as any).refreshCount || 0) < 3) {
  return tryRefreshToken().then((token) => {
    ;(error.config as any).refreshCount = ((error.config as any).refreshCount || 0) + 1
    error.config.headers = { ...error.config.headers, Authorization: `Bearer ${token}` }
    return request(error.config) // 带新令牌原样重试
  })
}
```

> **为什么计数器挂在 `error.config` 而不是全局变量**：每个请求的 401 重试次数应当独立统计；axios 的 config 对象在重试链中原样传递，天然是"随请求走"的载体。全局变量会导致 A 请求的重试耗尽配额影响 B 请求。

**③ 业务 401 分流 —— 解决误登出**

```typescript
function isBusiness401(url = '') {
  return (
    url.includes('/auth/login') || url.includes('/auth/password') || url.includes('/auth/logout')
  )
}
```

> **语义区分**：登录/修改密码返回 401 是"凭据有误"，退出登录返回 401 是"会话已清理"——它们都不是"令牌过期"。若走刷新链：改密码输错旧密码会把用户登出；登出请求 401 会反向触发刷新 → 死循环。

**④ 匿名 401 下抛 —— 解决整页 reload 死循环**

```typescript
const hadToken = Boolean(error.config?.headers?.Authorization)
if (!hadToken) return Promise.reject(error) // 未带 token 的 401 直接下抛
```

配合 `forceLogout()` 里的路径守卫（已在 `/login` 时不再赋值 `location.href`，避免同 URL 赋值触发 reload）。

> **场景还原**：登录页会请求设计为公开但后端白名单未放行的接口（如 `/public/statistics`），匿名 401 → forceLogout → 整页 reload → 又请求 → 又 401……约 150ms 一次。修复：匿名 401 不走刷新/登出，下抛给调用方 catch 静默处理。

**⑤ 附带细节：令牌回写保源**

刷新成功回写新令牌时用 `writeTokenPreservingSource()`——沿用旧 token 的存储位置（localStorage vs sessionStorage），避免勾了「记住我」的用户刷新一次令牌就降级成会话级登录。

#### 结果

提交「2026/09/07-前端-退出登录 401 死循环修复与接口去重治理」（15 文件 +440/-110），登录态相关零回归。

---

### 4.2 【性能】Store 双层请求去重 —— 全局推广的模式

**代码位置**：`src/app/stores/submission.ts`（最典型，带 5 步注释）、`archive.ts` 等 9 个 Store

#### 背景

多个页面/组件共享同一 Store，onMounted 各自 fetch：并发时同一接口连发多次；切换页面又对 60 秒内已拉过的数据重复请求。

#### 方案：两把锁解决两类问题

```typescript
let pendingFetch: Promise<void> | null = null // 锁1：防并发重入
let lastFetchedAt = 0 // 锁2：防跨页冗余
const CACHE_TTL_MS = 60_000

async function fetchRecords(filters?: SubmissionFilters, force = false) {
  if (pendingFetch) return pendingFetch // ① 在飞的直接复用

  const isFiltered = !!filters
  if (
    !force &&
    !isFiltered &&
    Date.now() - lastFetchedAt < CACHE_TTL_MS &&
    records.value.length > 0
  ) {
    // ② TTL 内用缓存
    filteredRecords.value = records.value
    return
  }

  pendingFetch = (async () => {
    // ③ IIFE 整段包进 Promise
    const raw = isFiltered ? await getSubmissionRecords(filters) : await getSubmissionRecords() // ④ 带条件只发一次
    if (!isFiltered) {
      records.value = raw // ⑤ 只有全量才写缓存+时间戳
      lastFetchedAt = Date.now()
    }
  })()
  try {
    await pendingFetch
  } finally {
    pendingFetch = null
  } // 完成后清理
}
```

> **两层各自解决什么**：`pendingFetch` 管"同一时刻"的并发（微任务层面的合并）；`lastFetchedAt + TTL` 管"一段时间内"的跨页冗余（页面 A 拉过，60s 内切到页面 B 不再拉）。
>
> **为什么 filtered 不写缓存**：过滤结果是瞬态数据，缓存它容易过期失真；且原来带 filters 会先发全量再发过滤两次请求，治理成**只发一次条件请求**。

#### 附加亮点：撤回状态的前端持久化

后端撤回语义是把记录置回草稿（`status=0`、可编辑），但产品要求撤回后**不可再编辑**。方案：本地维护 `withdrawnIds` 写入 localStorage，每次重拉列表用 `applyWithdrawnMarks()` 强制标回「已撤回、不可编辑、可删除」——在后端语义不变的前提下兜住前端交互契约。

---

### 4.3 【原创】成长时间轴模块 —— 从 0 到 1 的完整设计

**代码位置**：`src/features/growth-timeline/`（21 个文件，约 4,700 行；12 次提交中 10 次为本人）

#### 架构分层

```
GrowthTimeline.vue（主页面：布局与数据流编排）
├── components/                     # 17 个组件
│   ├── HeroRings.vue (498行)      # 原子轨道主视觉总装
│   │   ├── HeroRingsNucleus.vue   #   原子核（学生核心画像）
│   │   ├── HeroRingsOrbit.vue     #   轨道环（能力维度）
│   │   ├── HeroRingsElectrons.vue #   电子（能力得分，沿轨道运动）
│   │   ├── HeroRingsParticles.vue #   背景粒子
│   │   └── HeroRingsTooltip.vue   #   悬浮详情
│   ├── TimelineNode.vue / TimelineListView.vue / GrowthCard.vue (491行)
│   ├── ExperienceDetail.vue (337行) / ExperienceForm.vue
│   └── ProgressDots / SummarySection / GrowthQuote / GrowthFooter
└── composables/                    # 4 个组合式函数（逻辑/视图分离）
    ├── useGrowthTimeline.ts        # 时间轴编排：加载/新增/合并/选中
    ├── useGrowthDataSources.ts     # 跨模块数据同步（申报→时间轴）
    ├── useScrollReveal.ts          # 滚动显现动画
    └── useRingEffects.ts           # 轨道动效
```

#### 技术点 1：后端优先 + 本地降级的数据策略

```typescript
const backendLoaded = ref(false)

function rebuildExperiences() {
  const base = backendLoaded.value ? backendExperiences.value : INITIAL_EXPERIENCES
  experiences.value = sortExperiencesByDate([...base, ...localExperiences.value])
}

async function loadBackendTimeline() {
  try {
    const data = await getGrowthTimeline()
    backendExperiences.value = (data.timeline ?? []).map(mapBackendEvent)
    backendLoaded.value = true
  } catch {
    backendLoaded.value = false // 后端不可用 → 回退本地示例数据，页面不白屏
  } finally {
    rebuildExperiences() // 统一走同一个合并入口
  }
}
```

> 接口联调期后端不稳定，这套「后端优先、失败降级本地」的策略保证了前端可以独立开发和演示。

#### 技术点 2：乐观更新 + 真实 ID 回填

新增经历先以 `local-${Date.now()}` 临时 ID 立即上屏，再异步 `POST /profile/growth-timeline`；**成功后用后端真实 ID 回填**（下次进入详情/编辑不丢关联），失败则保留本地经历并提示。字段映射层 `mapBackendEvent()` 统一处理后端驼峰字段（`eventName→title`、`eventAt→date`、`sourceId→recordId`）与学期推断。

#### 技术点 3：IntersectionObserver 滚动显现的两个坑

```typescript
// 坑1：项目滚动容器不是 window，而是布局的 .layout__content
function findScrollRoot() {
  return document.querySelector('.layout__content')
}

observer = new IntersectionObserver(callback, { threshold, rootMargin, root })
```

```typescript
// 坑2：双 rAF——先让浏览器绘制出 opacity:0 的初始态，再触发过渡，
// 否则元素"出生即可见"，动画看起来丢失
requestAnimationFrame(() => {
  requestAnimationFrame(() => {
    isVisible.value = true
  })
})
```

> 还做了三处防御：`IntersectionObserver` 不存在时直接视为可见（降级）；`setTimeout` 延迟 observe 等 DOM 布局稳定；`once` 模式下触发后 `disconnect()` 释放观察器。**「动画丢失修复+强制全局启用」就是专门修这类时序问题的提交（+349 行）。**

#### 技术点 4：跨模块数据同步

`useGrowthDataSources` 把申报模块的记录（经审核通过的）同步为时间轴经历：

- `sanitizeSyncedText()`：正则清洗同步文本（移除 URL 路径、纯英文片段）
- `inferSkillsByType()`：按类型关键词推断能力维度成长值
- 自动同步开关持久化到 localStorage（`growth-timeline:auto-sync`）

---

### 4.4 【架构】useApplicationForm —— 泛型消灭 10 处重复

**代码位置**：`src/shared/composables/useApplicationForm.ts`（唯一作者）

#### 背景

10 个申报模块（读书报告/证书/竞赛/创新/实习/组织/科研/奖学金/实践/培训）结构高度同构：reactive 表单 + 必填校验 + 提交 loading 锁 + 成功提示 + 重置，每处约 170 行复制粘贴代码。

#### 方案

```typescript
export interface ApplicationFormOptions<T> {
  emptyForm: () => T // 空表单工厂：重置逻辑统一
  requiredFields?: (keyof T & string)[] // 必填字段声明式配置
  onSubmit?: (form: T) => Promise<void> | void // 默认走 submitApplication
  type?: string
  typeLabel?: string // 申报类型标识/中文名
  successMessage?: string
}

export function useApplicationForm<T extends Record<string, any>>(opts) {
  const form = reactive(emptyForm())
  const submitting = ref(false) // 提交中状态锁（防重复提交）

  function validateRequired(): boolean {
    const emptyFields = requiredFields.filter((key) => {
      const value = form[key]
      if (value == null || value === '') return true
      if (Array.isArray(value) && value.length === 0) return true // 空数组也算空
      return false
    })
    if (emptyFields.length > 0) {
      ElMessage.warning('请填写所有必填项后再提交')
      return false
    }
    return true
  }

  async function handleSubmit() {
    if (submitting.value) return // 双击防护
    if (!validateRequired()) return
    submitting.value = true
    try {
      await onSubmit(toRaw(form) as T) // toRaw：剥离响应式代理再提交
    } finally {
      submitting.value = false
    }
  }
  // ...
}
```

> **为什么用泛型**：每个模块的表单结构不同（`T extends Record<string, any>`），泛型让 `form.name`、`form[key]` 在使用侧保留完整类型提示，而不是 `any` 一路裸奔。
>
> **为什么 `toRaw(form)`**：reactive 代理对象直接序列化会携带 Proxy，`toRaw` 取回原始对象再交给 API 层，也避免把响应式引用泄漏出组件边界。
>
> **效果**：每个申报模块减少约 170 行重复代码，且校验规则变更只改一处。

---

### 4.5 【工程】CI 门禁与规范体系

**代码位置**：`.github/workflows/ci.yml`（唯一作者）、`claude.md`（450 行）

```yaml
on: { push: { branches: [main, new-main] }, pull_request: { branches: [main, new-main] } }
jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: npm }
      - run: npm ci
      - run: npm run lint:check # eslint . --max-warnings 0 零警告策略
      - run: npx vue-tsc -b # 类型检查独立于 build 先跑，失败定位更清晰
      - run: npm run build # vite build
```

配套规范体系（本人撰写/落地）：

- **`claude.md`（450 行）**：团队 AI 协作开发规范——目录/命名、Vue 九段式骨架、单文件 ≤600 行、字典集中维护、Loading 状态矩阵、接口字段红线（禁止伪造字段/禁止 Mock 字段当真实接口）、AI 生成代码人工检查清单
- **husky + lint-staged**：提交前自动 `eslint --fix` + `prettier --write`；专项修复过 husky pre-commit 在 GUI 客户端（无 shell PATH）中静默失效的问题
- **代码修改记录制度**：`代码修改记录/YYYY-MM-DD.md`，先删后增、一次需求一份，个人累计产出约 20 份
- **结构治理**：拍平 features 单层 pages 目录（23 文件）、重命名全部同名 `index.ts`（72 文件）、清理无关文档

---

### 4.6 【对接】学生端接口 V5.8 全量接线

| 事项              | 内容                                                                                                                                                                                        |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 全量切换（08-25） | 29 文件 +1,248/-518：10 个 Store 全部 Mock→真实 API，Dashboard/时间轴/AI 助手/审批/申报/奖项 6 大块页面数据源收口 Store                                                                     |
| 缺口补全（08-25） | 13 文件 +2,853：补齐文档有但代码缺的 10 项——`PUT /auth/password` 修改密码、`/notifications/unread-count`、职业规划 `GET/PUT/DELETE /career-plan/{id}` 等；动态记录/提交记录分页参数对齐文档 |
| 类型统一          | 服务端分页统一 `PaginatedData<T>` + `PaginationParams` 泛型约束，7 个 API 模块（auth/user/archive/submission/notification/activity/career-plan）                                            |
| 代理修正          | 发现 dev 全量 404：Vite 代理 `/api` 与后端 `context-path /api/v1` 不一致，统一对齐（`vite.config.js` 代理 + `request.ts` baseURL + 15s 超时）                                               |

> **对接方法论的产出**：核对文档→补缺口→字段映射集中在 API 层（如 `mapBackendEvent`）→Store 收口→页面只消费 Store。「文档中存在但代码缺失」「参数不一致」两类问题被系统性清零（记录在 `docs/api.md`）。

---

### 4.7 【UI/UX】主题体系与体验专项（选讲）

| 专项                         | 亮点                                                                       |
| ---------------------------- | -------------------------------------------------------------------------- |
| 暗色模式全局适配（29 文件）  | CSS 变量双主题体系 + 表单校验提示统一                                      |
| 夜间模式对比度修复（7 文件） | 对比度可达性（WCAG）+ **按登录时间自动切换主题**（晚间登录直接进夜间模式） |
| Tab 栏抖动综合修复           | 切换动画导致的布局抖动根治 + 标签数量上限体验优化                          |
| Dashboard/语音输入（5 文件） | 图表渲染与 Web Speech API 语音输入修复                                     |
| 大规模清理（116 文件）       | Logo 更换 + 组件拆分 + 冗余文件清理                                        |

---

## 五、高频追问 Q&A（模拟考官视角）

**Q1：为什么刷新令牌要做成单飞？不做会怎样？**
并发 401 时（首屏十几个请求很常见），每个都触发 `/auth/refresh`。后端 refreshToken 是一次性的，第一个成功后其余全部失败 → 全体 forceLogout，用户"莫名被登出"。单飞让同一时刻只有一个刷新请求，其余复用其 Promise 结果。

**Q2：重试计数为什么挂在 `error.config` 上，不用全局变量或 Map？**
每条请求的重试预算应当独立。axios 重试时 `request(error.config)` 原样传递 config 对象，它天然是"随请求生命周期走"的载体——不需要额外维护 Map 的 key 和清理时机。全局计数器会让 A 请求的重试耗尽 B 的配额。（权衡：axios 类型未扩展该字段，用了 `as any` 并注释，属于已知技术债。）

**Q3：登出接口为什么要排除在 401 刷新链外？**
两个场景：① 账号已在服务端删除/会话已过期时登出返回 401，若触发刷新 → 刷新也 401 → 再登出 → 死循环；② 正常登出本来就是要清会话，此时"续命"逻辑没有意义。排除后登出失败只静默清本地。

**Q4：匿名 401（请求没带 Authorization 却返回 401）说明什么？**
说明该接口需要登录态但前端以匿名方式访问了（如设计为公开、后端白名单未放行）。这不是"令牌过期"——没有令牌可刷。正确处理是下抛给调用方 catch 静默，若走 forceLogout 会整页 reload 且反复触发（150ms 一次的 reload 死循环就是这么来的）。

**Q5：Store 去重为什么需要两层？TTL 缓存会不会导致数据不新鲜？**
`pendingFetch` 解决并发（同一时刻合并），`lastFetchedAt + 60s TTL` 解决跨页冗余（时间维度）。新鲜度问题：提供 `force = true` 参数强制穿透缓存（下拉刷新/ mutation 后重拉都走 force），默认导航进页面用缓存换体验。

**Q6：带筛选条件时为什么只发一次请求？原来是什么问题？**
原来 fetchRecords(filters) 先拉全量再发条件请求，两次网络往返且全量结果被过滤结果覆盖浪费。治理后：filtered 只发条件请求且**不写全量缓存**（瞬态数据不缓存，避免失真）；只有无条件的全量请求才更新 `records` 与时间戳。

**Q7：成长时间轴后端不可用时页面会白屏吗？**
不会。`loadBackendTimeline` 失败时 `backendLoaded = false`，`rebuildExperiences` 的合并基准回退到内置示例数据 `INITIAL_EXPERIENCES`，再拼上用户本地新增的经历——联调期后端不稳定时前端仍可完整演示。

**Q8：双 rAF 那个动画坑，具体是什么机制？**
元素初始 `opacity:0`，回调里直接置 `visible` 时，浏览器可能把"初始态"和"过渡态"合并在同一帧渲染，等于跳过了过渡。两层 `requestAnimationFrame` 强制先提交一帧初始态，下一帧再变更属性，过渡才可见。另外项目滚动根不是 window 而是 `.layout__content`，IntersectionObserver 必须显式传 `root`，否则永远不触发——这是当时"动画丢失"的主因之一。

**Q9：useApplicationForm 里 toRaw 是干什么的？**
reactive 返回的是 Proxy 代理。直接把代理传给 API 层：序列化开销大、可能把响应式系统泄漏到组件边界外。`toRaw(form)` 取回原始普通对象再提交。

**Q10：为什么 CI 要 `--max-warnings 0`？**
警告零容忍才能防止"今天一个 warning，半年后一千个"的规范腐化。警告即阻断合并，倒逼提交前本地修复；配合 husky 的 lint-staged，大部分问题在提交前就被自动 fix。

**Q11：撤回后为什么前端要自己维护 withdrawnIds？**
后端撤回的实现是把记录置回草稿（可编辑），但产品语义要求"撤回=终止本次申报，不可再编辑"。前后端语义暂时不一致时，前端用 localStorage 持久化撤回 ID 列表，每次重拉数据后 `applyWithdrawnMarks` 强制修正状态——这是一个**后端不动、前端兜住交互契约**的过渡方案。

**Q12：你这个项目有什么不足？**（主动示弱版）
① 无自动化测试，验证依赖 `vue-tsc` + build + 人工回归，像 401 死循环这种问题如果有 E2E 会更早暴露；② request.ts 的 `refreshCount` 用了 `as any`，正解是给 axios 模块做 declaration merging 扩展类型；③ 部分接口保留了 Mock 回退双链路，联调期是优点、长期是维护负担；④ 时间轴的能力成长值有前端按类型推断的兜底逻辑，理想态应全部来自后端能力雷达数据。

---

## 六、代表性提交清单（按讲述顺序）

| 日期  | 提交                                               | 规模             |
| ----- | -------------------------------------------------- | ---------------- |
| 07-01 | 按三本手册规范整改项目并提交修改记录               | 52 文件 +2,919   |
| 07-01 | 重命名重复 index.ts 并补齐 CI 与架构文档           | 72 文件 +6,000   |
| 07-02 | 重构申报与报名模块并抽取公共组件                   | 35 文件 +2,697   |
| 07-03 | 新增成长时间轴平面化页面与核心交互                 | 20 文件 +4,412   |
| 07-04 | 清理页面冗余、扩展模块架构、建设消息与动态中心     | 42 文件 +3,683   |
| 07-06 | 首页与档案等页面问题集中修复                       | 62 文件 +4,526   |
| 07-07 | 抽取 useApplicationForm 消除 10 个申报模块重复逻辑 | 12 文件          |
| 07-08 | 成长时间轴动画优化与 AI 助手等 UI 全面修复         | 59 文件 +3,857   |
| 07-08 | 档案编辑、草稿保存与奖项报名联动修复               | 25 文件 +877     |
| 07-10 | Logo 更换、代码规范修复、组件拆分与冗余清理        | 116 文件 +12,635 |
| 08-25 | 学生端接口文档 V5.8 全量接线（P0 五项+P1+P2）      | 29 文件 +1,248   |
| 08-25 | 十项接口缺口补全与代码修改记录                     | 13 文件 +2,853   |
| 09-07 | 退出登录 401 死循环修复与接口去重治理              | 15 文件 +440     |

---

## 七、速查数据卡（考前 5 分钟过一遍）

- 提交 **62** 次 / 4 人第 1 / 约 **44%** ｜ +67,425 行
- 成长时间轴 **21 文件 ~4,700 行**：17 组件 + 4 composables（HeroRings 主组件 498 行）
- 申报 **10** 模块 + 奖项 **3** 模块，`useApplicationForm` 每处省 **~170 行**
- 401 防护 **四层**：单飞 / 3 次上限 / 业务 401 分流（login·password·logout）/ 匿名 401 下抛
- Store 去重 **两层**：pendingFetch 并发 + lastFetchedAt × **60s** TTL，force 可穿透
- V5.8 接线 **29 文件**，缺口补全 **10 项 +2,853 行**，分页统一 `PaginatedData<T>`
- CI 三层：lint 零警告 → vue-tsc → build（Node 22，npm ci + cache）
- 技术栈版本：Vue **3.5** / Pinia **3.0** / Element Plus **2.9** / TS **5.7** / Vite **6.2** / axios **1.7**

---

> 生成时间：2026-09-08 ｜ 数据来源：`git log --author="nine-mirrors"`、`git shortlog -sne --all`、源码核读
