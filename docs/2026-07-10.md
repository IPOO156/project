# 代码修改记录 — 2026-07-10

## 一、删除范围

### 1. 废弃资源文件

- `src/assets/logo/logo-icon.jpg` — 旧 JPG logo，已被 PNG 替换
- `assets/logo/logo-icon(2).jpg` — 旧 logo 备份
- `assets/logo/logo-icon.png` — 与 src/assets 重复
- `--output` — 不明构建产物

### 2. 冗余共享代码（无引用）

- `src/shared/ui/GreetingDialog.vue` — 0 引用
- `src/shared/composables/useFormValidate.ts` — 0 调用
- `src/shared/composables/useExport.ts` — 0 调用
- `src/shared/composables/usePermission.ts` — 0 调用
- `src/shared/utils/format.ts` — 0 调用
- `src/shared/utils/utils.ts` — 空壳 barrel
- `src/shared/api/user.ts` — 0 调用
- `src/features/messages/components/ActivityRecordDialog.vue` — 0 引用

### 3. 重复配置文件

- `vite.config.js` — 与 vite.config.ts 完全重复

### 4. Barrel 导出清理

- `src/shared/composables/composables.ts` — 移除 useExport/useFormValidate/usePermission 导出
- `src/shared/api/api.ts` — 移除 userApi 导出

## 二、新增范围

### 1. Logo 替换

- `src/assets/logo/logo-icon.png` — 新增 PNG logo 文件
- `src/app/layouts/components/Sidebar.vue` — logo 引用从 .jpg 改为 .png，图标放大至 56px，夜间模式提亮
- `src/features/auth/Login.vue` — Canvas 绘制 logo 替换为 PNG 图片引入，夜间模式提亮

### 2. 代码规范修复

- `src/features/awards/AwardOverview.vue` — 硬编码内联样式迁移至 CSS 类
- `src/shared/composables/useUpload.ts` — `as any` 替换为 UploadResponse 类型
- `src/features/approval/components/StarSection.vue` — `as any` 替换为 Record<string, unknown>
- `src/features/approval/components/ReviewSection.vue` — `as any` 替换为明确类型断言
- `src/app/stores/tabs.ts` — 精简注释，218行 → 191行

### 3. 组件拆分（单文件 ≤ 600行）

| 原文件                           | 原行数 | 现行为 | 拆出子组件                                 |
| -------------------------------- | ------ | ------ | ------------------------------------------ |
| auth/Login.vue                   | 1458   | 349    | LoginParticles, LoginHero, LoginBackground |
| dashboard/Dashboard.vue          | 847    | 485    | StatsOverview, QuickEntries                |
| profile/ProfileInfo.vue          | 818    | 558    | DimensionPanel                             |
| messages/MessageCenter.vue       | 753    | 523    | MessageCard                                |
| approval/ReviewSection.vue       | 728    | 597    | ReviewDialog                               |
| approval/StarSection.vue         | 748    | 348    | StarDialog                                 |
| growth-timeline/TimelineNode.vue | 749    | 210    | GrowthCard                                 |
| growth-timeline/HeroRings.vue    | 697    | 577    | HeroRingsFooter                            |

### 4. 类型安全

- `as any` 全项目清零（4处→0）
- `@ts-ignore` 全项目 0 处
- `eslint-disable` 全项目 0 处
- `vue-tsc --noEmit` 通过

### 5. 运行时 Bug 修复

- `src/features/dashboard/Dashboard.vue` — ECharts 雷达图 `radarOption` 无数据时返回 `null`，模板只在 option 存在时挂载 VChart；`profileSummary` 空数组时隐藏右侧详情区
- `src/app/layouts/components/NavTabs.vue` — 关闭后台标签时不再执行 `router.push(fallback)`，仅关闭当前激活标签才跳转
- `src/features/ai-chat/composables/useVoiceInput.ts` — 增加 `isSecureContextForVoice` 计算属性，区分"浏览器不支持"和"非 HTTPS/localhost 环境"，错误提示更明确；补上 `onstart` 回调正确管理录音状态
- `src/features/ai-chat/components/ChatInput.vue` — 语音提示文案更新，`displayText` 计算属性控制实时回显与输入分离
