# 后端问题清单（需后端更改）

- **日期**：2026-09-11
- **提出方**：前端
- **紧急度**：含 3 项上线前必须处理（**P0-6 管理员账号无角色**、P0-2 数据侧、P0-5 鉴权层）
- **核对方式**：每条均在前端实测抓包或后端源码中定位到具体文件行号；未经验证的推断已单独标注

---

## P0-1｜线上库 `role_permissions` / `role_scopes` 未按种子初始化

**实测**（2026-09-11 抓包）：教师账号 `GET /api/v1/auth/me` → **HTTP 200**，响应体：

```json
{ "code": 0, "message": "success",
  "data": { "userId": 2, "userNo": "T20230001", "name": "李四",
            "roles": ["teacher"], "roleNames": ["教师"],
            "permissions": [], "scopes": [] } }
```

请求成功，是 `permissions` 与 `scopes` **双双为空**。

**影响**：管理员端菜单 19 → 1 项，教师端 5 → 1 项（仅存「日志查看」，因它挂着前端过渡通配码 `*`）。

**自检 SQL**：

```sql
SELECT r.code, COUNT(rp.id) AS grant_count
FROM roles r LEFT JOIN role_permissions rp ON rp.role_id = r.id
WHERE r.code IN ('admin','teacher','counselor') GROUP BY r.code;
-- 按种子期望：admin 17、teacher 14、counselor 14；实际若为 0 即命中

SELECT r.code, COUNT(rs.id) AS scope_count
FROM roles r LEFT JOIN role_scopes rs ON rs.role_id = r.id GROUP BY r.code;
```

---

## P0-2｜教师端日志：接口有校验，但 `log:view` 没授给教师角色

`GET /teacher/logs` → `TeacherLogService.listLogs()` 首行即校验（`service/Lzw/TeacherLogService.java:76`）：

```java
adminAuthService.requireAdminOrPermission(teacherId, "log:view");
```

`requireAdminOrPermission`（`service/common/AdminAuthService.java`）在非 admin 且 `permissions` 为空时直接抛 20005 —— **不存在越权**。

问题在授权数据：`log:view` 只在 `seed_admins.sql:73`（permission id 22）定义并授予 admin，`seed_teachers.sql` 中**没有**它。

**后果**：教师访问教师端日志必然 **20005**。前端为让菜单可见而挂的 `*` 只做出了「菜单可见」——**教师端目前唯一可见的菜单项点进去没有数据**。

**期望**：为 teacher / counselor 授予 `log:view`（或建教师端日志独立码并授予）。

---

## P0-3｜`role_scopes` 为空导致教师端首页统计接口必然 403

`GET /teacher/statistics/dashboard` → `TeacherStatisticsService.getDashboard()`。前端只传 `semesterId`，`scopeType` 为空即进入 `resolveDefaultScope()`（`service/Fmy/TeacherStatisticsService.java:212-216`）：

```java
List<RoleScope> scopes = scopeValidator.effectiveScopes(userId);
if (scopes.isEmpty()) {
    throw new BusinessException(ResultCode.ACCESS_DENIED, "无访问权限");
}
```

`ACCESS_DENIED` = 业务码 20005 / **HTTP 403**。

**后果**：教师端首页「档案数据概览」卡片**整块无数据**（前端 catch 后置空、不弹提示，表现为"没数据"而非"请求失败"）。此接口**不走权限码、只走 `role_scopes`**，所以前端的空权限兜底救不了它 —— 与菜单塌空同根因、不同表现。

**同批影响**：`useScopeFilter` 取不到学院/专业/班级，档案查看、成果热力图等页的范围筛选为空下拉。

---

## P0-4｜审批委托：种子授权方向与业务口径相反 + Controller 无校验

**业务口径**（产品确认）：审批委托只能「管理员 → 教师」，教师之间不可互相委托，教师端不应出现该模块。

1. **种子方向反了** —— `delegate:manage`（permission id 36）在 `seed_teachers.sql:99,113` 授予了 teacher(3)、counselor(4)，而 `seed_admins.sql:82-99` 中 admin 的授权**不含 36**；
2. **Controller 有声明无校验** —— `controller/Fmy/TeacherDelegationController.java` 的 Javadoc 标注「权限码：`delegate:manage`」，方法体内**未调用任何鉴权**；`/teacher/**` 在 `config/SecurityConfig.java:76` 仅要求 `authenticated`；
3. **Service 语义是「自己委托自己」** —— `service/Fmy/TeacherDelegationService.java:166-227` 的 `delegatorId` 取自当前登录人并校验「委托人须持有该 roleId + 范围覆盖」，管理员不持有 role 3 时传 `roleId=3` 会被拒；
4. **附带缺陷** —— 该页「被委托人」下拉调管理端 `GET /admin/users`，教师调用**必然 403**。

**期望**：明确 `delegate:manage` 归属（改授 admin 或新建管理员专属码）；补 Controller 校验；明确接口语义（`delegatorId` 是否固定为当前登录人、`roleId` 是否必传、`isAuditor` 是否收窄）；如需教师查看「委托给我的」记录，请提供独立码 + 教师端可访问的数据源。

---

## P0-5｜HTTP 层 `permitAll` 让 3 个业务控制器无需登录即可进入

`config/SecurityConfig.java:80` 为 `.anyRequest().permitAll()`，仅列在前面的前缀受保护。以下控制器**不在**受保护前缀内：

| 控制器 | 前缀 | 方法数 | 用 `@AuthenticationPrincipal` 的方法 |
| --- | --- | --- | --- |
| `Lzw/ApplicationController` | `/applications` | 15 | 14（`/{type}/guide` 无） |
| `Lzw/AwardController` | `/awards` | 12 | 11（`/{type}/guide` 无） |
| `Lzw/AiConversationController` | `/ai` | 8 | 8 |

**已核实**：`ApplicationService`、`AwardService`、`AiConversationService` 三个服务中 **`userId == null` 守卫数量均为 0** —— 未登录请求会以 `userId = null` 流入业务逻辑，下游行为取决于仓储层（`findByUserId(null)` 空结果，或插入 null 外键触发 500），**至少不是明确的 401**。

> **未核实边界（如实说明）**：我核实的是「鉴权层缺口 + 无 null 守卫」这条链路，**未做运行期验证**，因此**不下"可被未登录者读写数据"的结论**，请后端确认 `null` 流入后的实际行为。
>
> **另**：`/public/statistics` 是**有意的**免鉴权接口（登录页统计概览，`PublicStatisticsController` 注释已标明）；`/applications/{type}/guide`、`/awards/{type}/guide` 无 principal 但疑似有意（申请指南静态内容）。

---

## P0-6｜管理员的 `user_roles` / `role_scopes` / 联系信息全未初始化 —— 该账号在系统里**不是管理员**

**实测**（2026-09-11 17:09 抓包，管理员 **A00001** 登录后 `GET /api/v1/auth/me`，**HTTP 200**）：

```json
{ "code": 0, "message": "success",
  "data": { "userId": 6, "userNo": "A00001", "name": "管理员A",
            "roles": [], "roleNames": [],
            "permissions": [], "scopes": [],
            "email": null, "phone": null } }
```

`userId: 6` / `name: "管理员A"` 与 `seed_admins.sql:29` 的 A00001 完全对应 —— **账号本身是种子里的那个管理员，但四个关联表全空**。

**影响（比 P0-1 更重，请优先看这条）**：

- 该账号在系统里**不具备管理员身份**：`AdminAuthService.requireAdmin` / `requireAdminOrPermission` 的第一步 `userRoleRepository.findByUserId(6)` 即为空 → 直接抛 20005。**所有 `requireAdmin` 系服务对该账号全部返回 403**，不是「少了几个菜单」这种程度。
- 前端表现：`roles.includes('admin')` 的直通判定失效，`permissions` 又为空 → 管理端菜单塌到 **1 项**（只剩挂通配码 `*` 的「日志查看」），且点进去同样是 20005 → **「管理员登录后只有日志查看，点进去还报没有权限」**。
- `email` / `phone` 为 `null`，说明 `user_contact_infos` 也未插入。

**定位：`seed_admins.sql` 的执行在 `role_permissions`（第 98 行）处中断，其后的语句一条都没生效。**

该文件内的顺序是 `role_permissions`(98) → `user_roles`(127) → `role_scopes`(136) → `user_contact_infos`(143)，四段全在断点之后。同一模式也解释了 **P0-1**：教师端有 `user_roles`（来自 `seed_teachers.sql`）却无 `role_permissions` —— **两个种子文件恰好都断在 `role_permissions` 这个位置**。这正是 P0-1 自检 SQL 查得出「角色权限为 0」、却查不出「用户没有角色」的原因。

**⇒ 自检 SQL（按 user 查，补上 P0-1 按 role 聚合查不到的维度）**：

```sql
SELECT u.user_no,
       COUNT(DISTINCT ur.role_id)       AS role_cnt,
       COUNT(DISTINCT rs.id)            AS scope_cnt,
       COUNT(DISTINCT rp.permission_id) AS perm_cnt
FROM users u
LEFT JOIN user_roles ur       ON ur.user_id = u.id
LEFT JOIN role_scopes rs      ON rs.user_id = u.id
LEFT JOIN role_permissions rp ON rp.role_id = ur.role_id
WHERE u.user_no IN ('A00001', 'A00002', 'T00001', 'T00002', 'T00003')
GROUP BY u.user_no;
-- 期望（对照 seed_admins.sql / seed_teachers.sql）：
--   A00001 / A00002 → role_cnt=1（role_id=2 即 code='admin'）、scope_cnt=1、perm_cnt=17
--   若 role_cnt=0 即命中本条
```

**期望修复**：补齐 `seed_admins.sql` 第 98 行**之后**的全部语句 —— `role_permissions`(98～)、`user_roles`(127-129)、`role_scopes`(136-138)、`user_contact_infos`(143-145)。

**另请加固导入方式**：目前是**静默中断**（前几条 INSERT 成功、后面失败，脚本不报错也未回滚），前后端因此被误导了多轮。建议种子脚本改为可重复执行（`INSERT IGNORE` 或先 `DELETE` 再插），并在导入后核对每张关联表的行数是否符合种子预期。

---

## P1-1｜7 个权限码「代码在校验、`permissions` 表里没建」

以下码被 Service 层实际调用，但**种子 `permissions` 表中不存在**，永远不会出现在任何人的 `/auth/me.permissions` 里 —— **校验永久失效**（非 admin 用户必被拒；admin 靠角色直通才不受影响）：

| 权限码 | 校验位置 |
| --- | --- |
| `archive:view` | `service/Fmy/AdminArchiveService.java:101` |
| `statistics:view` | `service/Fmy/AdminStatisticsService.java:80` |
| `archive:export` | `service/Fmy/AdminExportService.java:145` |
| `form:template:manage` | `service/Fmy/AdminFormTemplateService.java:65` |
| `semester:import` | `service/Lzw/SemesterManageService.java:75` |
| `user:view` / `user:manage` | `service/Lzw/UserManageService.java:111` 等 |

**易混淆点（请一并裁定）**：`form:template:manage` 与已存在的 `export:template:manage` 极像；`user:manage` 与已存在的 `system:user:manage` 不一致。是建新码还是复用已有码？

---

## P1-2｜6 个管理端权限码「种子里建了、代码从不校验」

以下码已定义并授予 admin，但 **Java 代码中零引用**（`grep "\"<code>\"" src/main/java` 命中 0 个文件）。对应服务只用 `requireAdmin(...)` 判「是否 admin 角色」：

| 权限码 | 服务 | `requireAdmin` 调用次数 |
| --- | --- | --- |
| `dictionary:manage` | `DictionaryManageService` | 5 |
| `org:manage` | `OrgManageService` | 7 |
| `system:role:manage` | `RoleManageService` | 7 |
| `system:manage` | `SystemConfigManageService` | 6 |
| `audit:revoke` | `ScheduledTaskManageService` | 2 |
| `data:manage` | （无对应校验点） | — |

**后果**：这些模块**无法通过权限码授权给非 admin 角色**；权限表里这 6 个码是死数据。**期望**：要么补校验，要么确认「仅 admin」是设计意图并清理死码。

**同类情况已确认 17 个码在 Java 代码中零引用**（含 `ai:invoke`、`audit:approve`、`audit:batch`、`audit:pending`、`dashboard:view`、`student:view`、`delegate:manage`、`teacher:archive:edit`、`teacher:archive:view`、`student:archive:view` 等）。其中一部分可能由范围校验（`ensureStudentInScope`）替代，**是否属缺陷请后端按模块确认** —— 前端不据此下结论。

---

## P1-3｜2 个前端在调用、后端不存在的接口

对前端 252 个 `request.*` 调用与后端 247 条路由做了全量交叉核对，其中 243 条完全匹配。**确认为真缺失**的 2 条：

| 前端调用 | 位置 | 说明 |
| --- | --- | --- |
| `GET /archive/dimensions` | `src/shared/api/archive.ts:210` | 「多维度画像」。后端**无 `/archive/**` 控制器**（`grep @RequestMapping("/archive` 仅命中 `/admin/ability-dimensions`，路径不同）。前端注释还写着「见 docs/api.md 3.4」。调用方：`src/app/stores/archive.ts:167` |
| `PUT /ai/conversations/{conversationId}` | `src/shared/api/ai-chat.ts:272` | 「重命名会话」。后端 `AiConversationController.java:90` **只有 DELETE**，无 PUT。前端代码注释自述「后端接口未实现时 catch 吞掉，调用方已乐观更新本地标题」——即**明知未实现，靠乐观更新掩盖** |

---

## 二、前端过渡措施（后端无需处理）

- 前端已加**空数据兜底**：`permissions` 为空时按角色基线展示菜单（教师 5 项 / 辅导员 4 项，与改造前一致），并给用户一条非阻断提示。
- 该兜底**仅在 `permissions` 为空时生效**，后端把授权数据下发到位后**自动失效**，前端无需回滚。
- 但**「日志查看」的通配码 `*` 必须在上线前移除**，前置条件是 P0-2 为教师角色授予 `log:view`。

---

## 三、请后端回复

1. 线上库 `user_roles` / `role_permissions` / `role_scopes` 的实际条数（**P0-1 按 role 聚合的 SQL + P0-6 按 user 聚合的 SQL 都要跑**，前者查不出「用户没有角色」）；
2. P0-2 / P0-4 / P0-5 的修复时间点（是否赶上本次上线）；
3. P1-1 中 7 个缺失码的建码方案；P1-2 的权限码裁定结果；
4. P1-3 两个缺失接口是否实现（若 `/archive/dimensions` 已有替代接口，请告知路径）。
